{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "build:typesafe-form": "pnpm --filter typesafe-form build",
    "build:react": "pnpm --filter @typesafe-form/react build",
    "build:svelte": "pnpm --filter @typesafe-form/svelte build",
    "build": "pnpm run build:typesafe-form && pnpm run build:react && pnpm run build:svelte"
  }
}