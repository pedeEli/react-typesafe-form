{
  "name": "root",
  "version": "1.0.0",
  "scripts": {
    "build:base": "pnpm --filter @typesafe-form/base-temp build",
    "build:react": "pnpm --filter @typesafe-form/react-temp build",
    "build:svelte": "pnpm --filter @typesafe-form/svelte build",
    "build": "pnpm run build:base && pnpm run build:react && pnpm run build:svelte"
  }
}